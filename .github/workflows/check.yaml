name: Pull Request Checks

on: 
  pull_request:
     
jobs:
  dart-pipeline:
    uses: affinidi/pipeline-dart/.github/workflows/check.yaml@fix-failsafe-pana
    secrets: inherit
    with:
      flutter-version: '3.35.3'
      package: false
      pana_min_score: 160
      env_vars: |
        MEDIATOR_DID=${{ vars.MEDIATOR_DID }}
        MEDIATOR_DID_ALTERNATIVE=${{ vars.MEDIATOR_DID_ALTERNATIVE }}
        CONTROL_PLANE_DID=${{ vars.CONTROL_PLANE_DID }}
